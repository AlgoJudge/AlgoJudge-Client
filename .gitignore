# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Output of `npm run check:package`.
.package-check/
.exchange-check/

# Output of `npm run check:content`.
.content-check/
# Output of `npm run check:events`.
.events-check/

# Screenshots from `npm run check:ui`. The browser profiles moved out of here to
# the system temporary directory, beside the pid registry — see `browser.mjs`.
scripts/verify/out/

# Playwright writes these on a failure; the run itself is the record.
test-results/
playwright-report/
.zawodyweb-check/
